# LLM 服务依赖
# 推荐模式（Ollama 代理）：
#   仅需 aiohttp + fastapi，轻量级
# 直接推理模式：
#   需要 transformers + torch（~2GB），嵌入式设备慎用

aiohttp>=3.9.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# 可选：直接推理模式
# transformers>=4.45.0
# torch>=2.4.0
# accelerate>=0.34.0
